apiVersion: v1
kind: Service
metadata:
  name: {{ include "flashapp.fullname" . }}
  labels:
    {{- include "flashapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flashapp.selectorLabels" . | nindent 4 }}
